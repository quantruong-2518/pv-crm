# Bản dựng phải giống nhau ở mọi máy và trên CI.
engine-strict=true
# pnpm strict: package chỉ thấy dependency nó tự khai báo — chặn phantom dependency.
# Đây là lý do chọn pnpm thay npm workspaces: import nhầm không compile được.
shamefully-hoist=false
auto-install-peers=true
