// PLACEHOLDER — real product copy has not been provided. Replace before PV_MAS_ENABLED=true.
//
// Split out of `mas-shell.tsx` (not merged in as a co-located export) because
// that file exports the `MasShellEmail` component and `react-refresh/only
// -export-components` flags any non-primitive export sitting next to a
// component in the same `.tsx` file — `allowConstantExport` only covers
// string/number/boolean literals, not arrays. This file has no component
// export, so the rule doesn't apply here.
export const PLACEHOLDER_PARAGRAPHS: string[] = [
  'Pebble Vina giới thiệu dòng chip bán dẫn AI biên (edge AI) mới — nội dung minh hoạ, chưa phải thông số chính thức.',
  'Đây là đoạn văn mẫu thứ hai, đứng vào chỗ một đoạn giới thiệu sản phẩm thật sẽ nằm trước khi có bản mô tả chính thức.',
]
