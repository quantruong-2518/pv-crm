node_modules
dist
coverage
*.tsbuildinfo
.DS_Store
.vite
.eslintcache

# Transcript thiết kế — chứa dữ liệu thương mại, không đẩy lên repo công khai
chats/
.vercel
.env*
.neon

# Mẫu biến môi trường thì PHẢI theo repo — nó là tài liệu, không phải bí mật.
!apps/api/.env.example
!apps/web/.env.example

# PGlite — cơ sở dữ liệu chạy trong tiến trình, dữ liệu của máy dev.
#
# Dấu sao KHÔNG phải cho đẹp: mẫu cũ khoá đúng một tên thư mục, nên một lần
# chạy thử với `pglite://./.pglite-scratch` đẻ ra 1.106 file mà `git add -A`
# quét sạch vào index (28/08). Mọi biến thể `.pglite-*` đều là dữ liệu máy dev.
apps/api/.pglite*/
